var pieCost = 0;
function Pizza (size, toppingCost){
  this.size = size;
  this.toppingCost = toppingCost;
}

Pizza.prototype.price = function(array){
  var toppingsTota
  pieCost = parseInt(this.size) +
}
